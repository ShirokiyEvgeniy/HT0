package by.epam.trainingrpa.validator;

/**
 * This validator is used for defining problems in rooms (buildings).
 * Problems connected with illumination and free space
 * */
public interface Validator {
    String validate();
}
